Config = Config or {}

Config.Feature = {
    EnableDynamicRespawnTimer = true, -- เปิดการคำนวณเวลาเกิดใหม่แบบ dynamic
    EnableAmbulanceBlips = true, -- เปิดระบบ blip สำหรับทีมแพทย์
    EnableTalkRequest = true, -- เปิดระบบขอคุยระหว่างผู้เล่นกับแพทย์
    EnableEventMetrics = true -- เปิดเก็บตัวชี้วัดจำนวนเรียกใช้ event ฝั่งเซิร์ฟเวอร์
}
